# Pickle runs

One line per run. Reports and screenshots live under `docs/runs/*/evidence/`, ignored by git, and
only the latest one for the build now in the repository is kept; an older one stays only if it is
the sole proof of a check the latest did not repeat.

- 2026-09-21 09:03, `01-trousers-review`, mod DLL `EA8644DA…`, WSL: 3/3 passed, `exitReason passed`, and all three captures showed a pawn hauling its trousers, not wearing them (the `is wearing` barrier passed anyway; AB was inert without a seeded settings file). Green and worth nothing. Superseded by the trouser art (DLL `F624927E…`), evidence deleted 2026-09-23.
- 2026-09-24 11:03, pass `wdi-ab-general`, DLL `7FD0FEBF…`, WSL: `exitReason failed`, 28 scenarios (28 discovered across 4 features), 11 passed, 10 failed, 7 skipped. **All 11 passes are `01` and `02`; all 10 failures are `03-silhouettes` on an undefined step** (`{string} body type is {word}`, Pickle PR #32, not released), not on the mod; the 7 skips are `04`, XND-only, as designed. The pawn is now drafted and stationary ("Watching for targets", Gear tab lists `Cloth pants (normal)` under Apparel, Inventory empty): the wear barrier holds. The mod log confirms the direct patch route, "1 more garment shares the trouser art" (the review's first fix firing on a real def) and "18 textures read from General, 3 drawn onto AB's plain shells". The pawn is ~45 px tall: **nobody has judged how the trousers read.** Evidence trimmed to `evidence/kept/` (0.13 MB of 154).
- 2026-09-24 11:21, pass `wdi-xnd`, DLL `7FD0FEBF…`, WSL: `exitReason passed`, 28 of 28 scenarios played, 10 passed, 0 failed, 18 skipped, all skips by tag as designed (7 AB-only in 01, 10 in 03, the General one). XND is the supplier and its worn graphic is `Core/Things/…` (not AB's); the mod stayed out of the way, its log holding only the direct-patch line and nothing about trousers; the three XND captures are of a drafted stationary pawn. Same limit as the AB pass: the pawn is ~45 px tall, so **how the band treats XND's art has still not been looked at.** Evidence trimmed to `evidence/kept/` (0.13 MB of 62).
- 2026-09-24 ~16:1x, pass `wdi-xnd-general`, DLL `7FD0FEBF…`, WSL via the dispatcher: `exitReason passed`, 28 of 28 played, 11 passed, 0 failed, 17 skipped by tag. XND and General are both loaded, AB is not; the mod's log holds only the direct-patch line, so it stayed out of the way and made no def. Same 45 px pawn: the band on General's art via XND has not been looked at.
- 2026-09-24, pass `tools` (the bare pass, no optional mod), same DLL: `exitReason passed`, 28 of 28, 4 passed (the quiet scenario and the three Gear tabs), 24 skipped by tag. Log: the patch line only.
- 2026-09-24, pass `wdi-ab`, same DLL: `exitReason failed`, 28 of 28, 11 passed, 10 failed, 7 skipped. All 10 failures are `03-silhouettes` on the undefined step `{string} body type is {word}` (Pickle PR #32), as in `wdi-ab-general`. Log: "1 more garment shares the trouser art" and "**0 textures read from General, 18 drawn onto AB's plain shells**, lowered by 0.03, children 0.09": the option that draws details on AB's shells, on its own. `wdi-ab-general` gave 18 from General and 3 drawn, so the two art options do what each was written to do.
- 2026-09-24, pass `wdi-ab-band` (AB trousers, both art options off), same DLL, WSL via the dispatcher: `exitReason failed`, 27 of 27, 10 passed, 10 failed, 7 skipped. The 10 failures are `03-silhouettes` on the undefined step `{string} body type is {word}` (Pickle PR #32); the passes are `01`/`02` (AB supplies, worn graphic set, no warning, three Gear tabs). Log: the patch line only. Evidence trimmed to `evidence/kept/` (0.13 MB of 180).
- 2026-09-25 ~16:32, pass `wdi-ab`, filter `::a hulking woman`, DLL `7FD0FEBF…`, tree `4d5dd53`, WSL via the dispatcher: `exitReason passed`, 1 of 1 played, 1 passed. PickleTools' body-type step set Hulk on a woman and `has body type Hulk` read it back. Log: patch line, "1 more garment(s) share the trouser art", "0 read from General, 18 drawn onto AB's plain shells". The capture shows the pawn from the waist up only: the selection bracket, the name label and the hover tooltip cover the legs, and Larson and Morrison stand beside it, so **the trousers on a Hulk body have not been seen**. Kept: `summary.json`, the log lines and a 6x crop of the pawn (0.07 MB of 7.9).
- 2026-09-25 ~16:36, pass `wdi-ab`, filter `::a boy`, same build: `exitReason failed`, 1 of 1 played, 0 passed, 1 failed: `pawn 'S-Boy' should have body type Child; it has Male`. Setting the age to 8 does not give a child body: the pawn keeps its adult one. **The same holds for `01-trousers-review`'s child scenario (`Waistline-K`, `is 8 years old`): it photographed an adult body in Apparel_KidPants, never a child.** Kept: `summary.json`, `failure.txt`, log lines.
